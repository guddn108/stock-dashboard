name: News Update

on:
  push:
    branches: [main]
    paths-ignore:
      - 'index.html'   # 봇 커밋이 다시 트리거 안 되도록
      - 'data/**'
  schedule:
    # UTC 16:00 = KST 01:00 (매일)
    - cron: '0 16 * * *'
  workflow_dispatch: # GitHub에서 수동 실행 버튼

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run news update
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: python main.py --only news

      - name: Commit & Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html data/data.json
          git diff --staged --quiet || git commit -m "auto: news update $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push
